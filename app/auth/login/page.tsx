import { LoginForm } from '@/components/auth/login-form'

export const metadata = {
  title: 'Sign In - EcoSphere',
  description: 'Sign in to your EcoSphere account',
}

export default function LoginPage() {
  return <LoginForm />
}
